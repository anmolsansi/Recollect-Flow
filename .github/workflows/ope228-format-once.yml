name: OPE-228 format once

on:
  push:
    branches:
      - agent/ope-228-backup-export-recovery

permissions:
  contents: write

concurrency:
  group: ope228-format-once
  cancel-in-progress: true

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ope-228-backup-export-recovery
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write .
      - name: Commit normalized files
        run: |
          if git diff --quiet; then
            echo 'No formatting changes required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'style(ope-228): normalize recovery acceptance files'
          git push origin HEAD:agent/ope-228-backup-export-recovery
