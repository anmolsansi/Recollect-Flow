# OPE-224 implementation and approval record

Policy version: `2026-07-21.1`

Implemented:

- Explicit provider/data matrix in `SECURITY_PRIVACY_GOVERNANCE.md` and `PolicyService`.
- Unknown and all policy/quota/provider failures fail closed.
- Restricted categories never use hosted providers.
- Every created enrichment job records privacy snapshot, provider eligibility and policy version.
- Admin privacy override supports audited reprocess or derived-data purge.
- Capture responses include the effective sensitivity state.
- Production migration `0005` and Worker version `33065734-2b25-4a07-9a1f-eee5cd49ed19` are deployed. Neutral production tests verified Public → OpenRouter/Gemini fallback, Personal without consent → no AI, compliant Personal → OpenRouter with both privacy controls, and Sensitive → no AI; D1 recorded four current-policy audit events.
- The approved revision removes the local Ollama dependency, adds app-managed OpenRouter with Public Gemini fallback, and stamps credential source, hosted consent, ZDR and data-collection requirements on new jobs.

Owner approval recorded 2026-07-21:

- [x] Public content may use app-managed OpenRouter free models with Gemini free fallback.
- [x] Personal content may use a user-provided key, or app-managed OpenRouter only after explicit hosted-processing consent with ZDR and data collection denied.
- [x] Missing consent or a compliant provider fails closed.
- [x] Unknown content is not sent automatically; Sensitive and restricted content never use hosted AI.
- [x] Owner acknowledges previously transmitted provider data cannot be revoked.
