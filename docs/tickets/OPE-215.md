# TICKET OPE-215: Bootstrap RecollectFlow repository and local workflow

## Goal

Create a reproducible, zero-cost local Worker project.

## Why This Exists

Every later slice needs stable commands, structure, and gates.

## User-Facing Behavior

None; developer foundation only.

## Scope

Node/TypeScript/Hono/Wrangler/Vitest/ESLint/Prettier configuration, examples, CI, and README.

## Out of Scope

Production deployment, accounts, feature endpoints beyond health.

## Files to Inspect First

`README.md`, `package.json`, `wrangler.toml`, `.github/workflows/ci.yml`.

## Files to Create or Edit

The files above plus TypeScript/lint/format/test configuration and `.dev.vars.example`.

## Data Model

None.

## API Contract

`GET /api/v1/health` returns `{data:{status:"ok"},meta:{request_id}}`.

## UI States

None.

## Validation Rules

Node 22+; no committed secrets; strict TypeScript.

## Error Handling

Unknown routes use the standard `NOT_FOUND` envelope.

## Auth & Permissions

No production secrets in CI or Git.

## Dependencies

OPE-213 scope contract.

## Integration Contract

Wrangler entrypoint is `apps/worker-api/src/index.ts`; D1 binding is `DB`.

## Implementation Steps

Scaffold configs; add app entrypoint; add commands; add CI; document quick start; run quality gate.

## Test Cases

Format, lint, typecheck, unit tests, and local start/migration commands.

## Manual QA

Fresh install from README and call health endpoint.

## Acceptance Criteria

`npm run check` passes; local workflow needs no paid service; env example contains names only.

## Definition of Done

All acceptance checks pass and repo context reflects real paths.

## Do Not Change

Zero-cost-first constraint and `/api/v1` prefix.

## Sequencing Rules

Complete before OPE-216.

## Build Order

1. Config 2. app shell 3. CI/docs 4. verification.

## Puzzle-Piece Test

OPE-216 can add D1 without changing the app boundary or commands.
